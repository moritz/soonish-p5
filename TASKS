TODO:
    * channels:
        + list channels
        + modify existings channels
        + UI safeguard for channel deletion
    * prettier layout
    * unified input/search field for city/country/zipcode
    * overview pages per
        + (?) zipcode
    * apply at partner programs

DONE:
    * channels:
        + give a search a name, thus creating a channel
        + delete channel
        + channel feeds

    * overview pages per
        + artist
        + location

    * basic multi-country search
    * proximity search by zipcode
    * imprint link
    * event detail view
